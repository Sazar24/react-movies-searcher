import * as React from 'react';

class SearchHistory extends React.Component {
    render() {
        return (
            <div>I am component to search through already found movies </div>
        );
    }
}

export default SearchHistory